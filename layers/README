To add layers, make sure each layer has the following properties:
- self.layers: an integer that is at least 1
- self.surface: an object of the form pygame.Surface. Usually, this can be achieved by getSurface(#layers)
